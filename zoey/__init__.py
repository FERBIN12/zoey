"""Zoey — a tiny, file-based personal AI agent runtime.

A stable identity, persistent memory, and proactive behavior on top of any LLM.
The whole point: the agent wakes up fresh each session, and a handful of markdown
files *are* its memory. See the docs/ folder for the architecture.
"""

__version__ = "0.1.0"
